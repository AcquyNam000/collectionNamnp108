mport java.util.*;

public class main {
        public static void main(String []arg)
        {
                int n = 0;
                int a = 0;
                Scanner sc = new Scanner(System.in);
                ArrayList<Integer> array = new ArrayList<>();
                System.out.println("Nhập số phần tử của mảng ");
                n = sc.nextInt();
                for(int i = 0 ; i < n ; i ++)
                {
                    System.out.print("nhập phần tử thứ"+ i + ":");
                    a = sc.nextInt();
                    array.add(a);
                }

                System.out.println("in các phẩn tử đã nhập");
                System.out.println(array);
                array.sort(Comparator.comparingInt(o -> (int) o).reversed());
                Set<Integer> hashset = new HashSet<>();
                System.out.println("Mảng đã sắp xep trong bo qua trung lap la ");
                for(int i = 0 ; i < array.toArray().length ; i ++)
                {
                    hashset.add(array.get(i));
                }
                System.out.println(hashset);

        }

}


